# Copy to `.env` in **this folder** (`MarryBackend/`) — not project root.

# Google OAuth 2.0 — **Web client** ID from Google Cloud Console → APIs & Services → Credentials
# Must match the frontend: REACT_APP_GOOGLE_CLIENT_ID in MarryTube/.env (same client id).
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Optional alias (either one works):
# GOOGLE_WEB_CLIENT_ID=

JWT_SECRET=
DB_HOST=
DB_NAME=
DB_USER=
DB_PASSWORD=
